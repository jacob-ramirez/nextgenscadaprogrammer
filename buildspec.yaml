version: 0.2

phases: 
    install:
        commands:
            - echo "installing something"
    pre_build:
        commands: 
            - echo "we are in the pre build phase"
    build:
        commands:
            - echo "we are in the build block"
            - echo "we will run some tests"
            - grep -ER '<title>Next Gen SCADA Programmer Blog &#8211; Jacob Ramirez|<p class="site-description">Jacob Ramirez' /*.html
    post_build:
        commands:
            - echo "we are in the post build phase"